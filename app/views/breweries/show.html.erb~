<p id="notice"><%= notice %></p>

<h2> <%= @brewery.name %> </h2>

<p>
  <i>Established in <%= @brewery.year %></i>
</p>
<p>
Has <%= pluralize(@brewery.beers.count, 'beer') %> with <%= pluralize(@brewery.ratings.count, 'rating') %>.
 Average rating of this brewery's products: <%= @brewery.average_rating %>
	<ul>
	
 		<% @brewery.beers.each do |beer| %>
   		<li><%= link_to beer.name, beer %></li>
 		<% end %>
	</ul>
<p>
<% if current_user %>
	<%= link_to 'Edit', edit_brewery_path(@brewery.id), class:'btn btn-primary' %></td>
        <%= link_to 'Destroy', @brewery, method: :delete, data: { confirm: 'Are you sure?' }, class:'btn btn-danger' %>
<%end%>
</p>
